package ir.sharif.pvz.model.game;

/**
 * The plant categories defined by the project document; the category decides
 * how a plant behaves each tick.
 */
public enum PlantCategory {
    SUN_PRODUCER,
    SHOOTER,
    LOBBER,
    EXPLOSIVE,
    MELEE,
    WALL,
    MODIFIER,
    STRIKE_THROUGH,
    HOMING,
    MINT,
    TRAP
}
