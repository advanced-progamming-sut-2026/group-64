package ir.sharif.pvz.view.fx.widget;

/**
 * The fixed size the lawn is drawn at. The battle screen scales the whole
 * canvas to the window, so everything on it keeps its proportions.
 */
public final class GameGeometry {

    /** Matches the aspect ratio of the extracted background art. */
    public static final double WIDTH = 1024;
    public static final double HEIGHT = 768;

    private GameGeometry() {
    }
}
